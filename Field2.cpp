#include "Field2.h"

Field2::Field2(QObject *parent) : QObject(parent)
{

}

/*void Field2::adjuctCardsPosition(int lane)
{
    int centralX = 280;
    for(int i = 0; i<cardAmount; i++)
    {
        card[cardAmount]->setPos(centralX - cardAmount*50 + 100*i, 100 + lane*50);
    }
}*/
